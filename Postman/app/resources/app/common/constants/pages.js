const SCRATCHPAD = 'scratchpad';
const OPEN_WORKSPACE_IDENTIFIER = 'workspace.open';
const HOME_IDENTIFIER = 'home';
const DEFAULT_HOME_IDENTIFIER = 'home.default';

module.exports = {
  DEFAULT_HOME_IDENTIFIER,
  HOME_IDENTIFIER,
  OPEN_WORKSPACE_IDENTIFIER,
  SCRATCHPAD
};
